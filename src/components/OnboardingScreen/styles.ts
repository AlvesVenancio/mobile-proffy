import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    header: {
        height: 300,
        alignItems: 'center',
        padding: 50,
        paddingTop: 60,
    },    
    body: {
        
    },
    buttonText: {
        color: 'red'
    }
});

export default styles;